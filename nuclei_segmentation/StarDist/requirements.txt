setuptools
numpy==1.26.4
matplotlib>=3.7.0
pandas>=2.0.0
opencv-python>=4.8.0
scipy>=1.11.0
tiffslide==1.5.0
# tiffslide 1.5.0 references tifffile.TIFF.RESUNIT, removed in tifffile>=2025.x.
# Newer tifffile makes tiffslide crash -> wrapper fallback hides the pyramid and
# forces a full-resolution thumbnail read (multi-minute stall). Pin to a version
# that still exposes RESUNIT so the pyramid loads.
tifffile<2025
multiprocess==0.70.17
# Pin to match the cloud stardist env. 2.0–3.0 range loads legacy `binf`
# classifiers and rebuilds n_classes_ on load; 1.7.x breaks predict on a
# loaded model, 3.1+ can't load binf at all.
xgboost==2.1.4
scikit-learn>=1.3.0
tqdm==4.64.1
natsort==8.2.0
colorama>=0.4.6  # For better Unicode support on Windows
einops>=0.7.0  # Required for transformers

# Dependencies for feature pre-calculation:
tensorflow==2.14.0; platform_system != "Darwin"  # for Windows and Linux
tensorflow==2.16.1; platform_system == "Darwin"  # for macOS
stardist==0.9.1
fastdist==1.1.6
sse-starlette
fastapi
uvicorn
transformers
tissuelab_sdk==0.1.12
czifile
pydicom
torch
# Windows-only dependencies; skip on macOS
pywin32; platform_system == "Windows"
pylibCZIrw; platform_system == "Windows"