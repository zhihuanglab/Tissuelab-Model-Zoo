# Core Python packages
numpy>=1.24.0,<2.0.0  # Pin to <2.0 for compatibility with stardist/numba
scipy>=1.11.0
matplotlib>=3.7.0
scikit-image>=0.21.0
scikit-learn>=1.3.0
pandas>=2.0.0
tqdm>=4.64.0
requests>=2.31.0

# PyTorch - install based on your system
# For CUDA 11.8: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# For CUDA 12.1: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# For CPU-only: pip install torch torchvision
torch>=2.0.0
torchvision>=0.15.0

# GPU Acceleration (required for fast contour/centroid extraction)
# Install cupy matching your CUDA version:
# For CUDA 12.x: pip install cupy-cuda12x
# For CUDA 11.x: pip install cupy-cuda11x
cupy-cuda12x>=13.0.0  # GPU contour ray-casting kernel
pyvips>=2.2.0  # Fast image region reading for embeddings

# Computer Vision
opencv-python>=4.8.0

# Image I/O
tiffslide>=1.5.0
tifffile>=2023.1.0
bioio>=0.4.0
rasterio>=1.3.0
pydicom  # For DICOM file support

# Data Storage
zarr==3.1.5

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
sse-starlette>=1.6.5

# Utilities
multiprocess>=0.70.17
fastremap>=1.1.6
geojson>=3.1.0
natsort>=8.2.0

# ====================================
# StarDist Embedding Dependencies
# (Required for PLIP-based nuclei embeddings via StarDist's NucleiEmbedding)
# ====================================
transformers>=4.30.0
einops>=0.7.0
fastdist>=1.1.6
stardist>=0.9.1
tissuelab_sdk>=0.1.12

# Optional dependencies (uncomment if needed)
# czifile  # For Carl Zeiss CZI file support
# bioio-bioformats>=0.1.0  # For bioformats image reader
# slideio>=0.4.0  # Alternative slide reader
# xgboost>=1.7.4  # Only if using XGBoost classifiers
