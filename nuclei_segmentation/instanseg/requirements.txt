# InstanSeg task node. Python 3.11.
# Pins match TissueLab/app/service/requirements-packaging.txt where used.
# tissuelab_sdk 0.1.14 already depends on nibabel and pydicom.

numpy==2.4.6
Pillow==12.3.0
opencv-python==5.0.0.93
scipy==1.17.1
matplotlib==3.11.1
zarr==3.1.6
tifffile==2026.3.3
tiffslide==3.0.1
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
requests==2.34.2
tissuelab_sdk==0.1.14
scikit-learn==1.5.2
scikit-image==0.26.0
sse-starlette==3.4.6

multiprocess>=0.70.17
fastremap>=1.1.6
einops>=0.7.0
fastdist>=1.1.6
numba
tqdm>=4.66.0

# PyTorch — keep torch/torchvision paired.
# CUDA: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch
torchvision

transformers>=4.30.0

# Imported at module load by instanseg.utils.utils (inference_class → pytorch_utils).
rasterio>=1.4.0
geojson>=3.1.0

# -----------------------------------------------------------------------------
# Optional — not needed for the default TissueLab tasknode path.
# -----------------------------------------------------------------------------
# bioio>=0.4.0                 # image_reader=bioio
# bioio-bioformats>=0.1.0      # image_reader=bioformats
# cupy-cuda12x>=13.0.0         # GPU contour ray-casting (Linux+CUDA only)
# pyvips>=2.2.0                # read_image_method=vips
# czifile                      # Carl Zeiss CZI
# slideio>=0.4.0               # alternative slide reader
# stardist>=0.9.1              # training metrics only (stardist.matching)
