# InstanSeg tasknode runtime deps (default path: tiffslide + zarr + FastAPI).
# numba (via optional stardist tooling) requires numpy<2.5.
numpy>=2.0,<2.5
scipy>=1.13.0
matplotlib>=3.8.0
scikit-image>=0.21.0
scikit-learn>=1.3.0,<1.6
pandas>=2.1.0
tqdm>=4.66.0
requests>=2.31.0
opencv-python>=4.8.0
multiprocess>=0.70.17
fastremap>=1.1.6
einops>=0.7.0
fastdist>=1.1.6
h5py

# PyTorch — keep torch/torchvision paired.
# CUDA: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch
torchvision

# Image I/O + storage (default reader is tiffslide)
tiffslide==3.0.0
tifffile>=2025.5.21
zarr==3.1.5

# Tasknode serving
fastapi>=0.141.0
uvicorn[standard]>=0.52.0
sse-starlette>=3.4.0

# Nuclei embedding (PLIP / transformers path)
transformers>=4.30.0

# -----------------------------------------------------------------------------
# Optional — not needed for the default TissueLab tasknode path.
# Uncomment only if you use the corresponding feature.
# -----------------------------------------------------------------------------
# bioio>=0.4.0                 # image_reader=bioio
# bioio-bioformats>=0.1.0      # image_reader=bioformats
# rasterio>=1.3.0              # save_geojson / labels_to_features
# geojson>=3.1.0               # save_geojson export
# cupy-cuda12x>=13.0.0         # GPU contour ray-casting (Linux+CUDA only)
# pyvips>=2.2.0                # read_image_method=vips
# pydicom                      # DICOM wrappers
# czifile                      # Carl Zeiss CZI
# slideio>=0.4.0               # alternative slide reader
# stardist>=0.9.1              # training metrics only (stardist.matching)
# xgboost==3.0.5               # if using XGBoost classifiers (stay on 3.0.x for binf)
