setuptools
# numba (via stardist) requires numpy<2.5; zarr/scipy are fine with 2.x.
numpy>=2.0,<2.5
matplotlib>=3.8.0
pandas>=2.1.0
opencv-python>=4.8.0
scipy>=1.13.0
Pillow
# tiffslide 3.x needs tifffile>=2025; do NOT keep the old tifffile<2025 pin.
tiffslide==3.0.0
multiprocess  # PyInstaller mp_hook (Windows spawn)
zarr==3.1.5
scikit-learn>=1.3.0
tqdm>=4.66.0
requests>=2.31.0

# Optional StarDist geometry (lazy-imported; embedding-only runs can skip these).
# Lower bound 2.14 (works on py3.11); pip will resolve to 2.20+ on py3.13 automatically.
tensorflow>=2.14.0
stardist>=0.9.1
fastdist==1.1.6  # imported by nuc_stat (loaded by nuc_embedding)

# Task node serving + H-optimus embedder
sse-starlette>=3.4.0
fastapi>=0.141.0
uvicorn[standard]>=0.52.0
timm>=1.0.0  # H-optimus-0 backbone (override CYTOFORMER_BACKBONE=uni2 for legacy)
# Default PyPI macOS/CPU builds; for CUDA:
# pip install torch --index-url https://download.pytorch.org/whl/cu124
torch

# Slide format extras (via tissuelab_sdk wrappers)
czifile
pydicom
# Windows-only CZI support
pywin32; platform_system == "Windows"
pylibCZIrw; platform_system == "Windows"
