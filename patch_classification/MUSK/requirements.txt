# MUSK patch-embedding + classification task node. Python 3.11.
# Pins match TissueLab/app/service/requirements-packaging.txt where used.

numpy==2.4.6
Pillow==12.3.0
opencv-python==5.0.0.93
zarr==3.1.6
tifffile==2026.3.3
tiffslide==3.0.1
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
requests==2.34.2
xgboost==3.0.5
pandas==3.0.5
scikit-image==0.26.0
sse-starlette==3.4.6

timm==0.9.8
einops
pyarrow
transformers<5
sentencepiece
fairscale
huggingface-hub
safetensors
datasets
tqdm>=4.66.0

# Keep torch/torchvision paired (embedding node imports torchvision.transforms).
# Default PyPI macOS/CPU builds; for CUDA:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch
torchvision
