setuptools
numpy>=2.0,<2.5
pandas>=2.1.0
zarr==3.1.5
# 3.0.x still loads legacy `binf` and rebuilds XGBClassifier.n_classes_ on load.
# 1.7.x loads binf but does NOT rebuild n_classes_ (breaks predict_proba);
# 3.1+ drops binf loading entirely. 3.0.5 is the highest binf-capable release.
xgboost==3.0.5
# Cap below 1.6: sklearn 1.6+ drops class-level `_estimator_type`, which
# breaks xgboost 3.0.5's XGBClassifier.save_model used by this tasknode.
scikit-learn>=1.3.0,<1.6
requests>=2.31.0
timm>=1.0.0  # needed to construct CellClassifier when loading the per-organ head
sse-starlette>=3.4.0
fastapi>=0.141.0
uvicorn[standard]>=0.52.0
# Default PyPI macOS/CPU builds; for CUDA:
# pip install torch --index-url https://download.pytorch.org/whl/cu124
torch
