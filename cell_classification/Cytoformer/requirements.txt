setuptools
numpy>=2.0,<2.5
matplotlib>=3.8.0
pandas>=2.1.0
opencv-python>=4.8.0
scipy>=1.13.0
tiffslide==3.0.0
multiprocess
zarr==3.1.5
# Align with NuClass / patch classifiers / Ctrl-Service. 3.0.x still loads
# legacy `binf` and rebuilds XGBClassifier.n_classes_ on load. 1.7.x loads
# binf but does NOT rebuild n_classes_ (breaks predict_proba); 3.1+ drops
# binf loading entirely. 3.0.5 is the highest binf-capable release.
xgboost==3.0.5
# Cap below 1.6: sklearn 1.6+ drops class-level `_estimator_type`, which
# breaks xgboost 3.0.5's XGBClassifier.save_model used by this tasknode.
scikit-learn>=1.3.0,<1.6
tqdm>=4.66.0
natsort>=8.4.0
requests>=2.31.0
# transformers is no longer used (PLIP text zero-shot was replaced by the
# Cytoformer per-organ head); kept only if other node code still imports it.
transformers
timm>=1.0.0  # UNI2-h backbone definition, needed to construct the per-organ head
sse-starlette>=3.4.0
fastapi>=0.141.0
uvicorn[standard]>=0.52.0
h5py
# Keep torch/torchvision paired. Default PyPI macOS/CPU builds; for CUDA:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch
torchvision
