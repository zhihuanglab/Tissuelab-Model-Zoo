# BiomedParse task node. Python 3.11.
# Pins match TissueLab/app/service/requirements-packaging.txt where used.
# Model-specific pins match the BiomedParse checkpoint.
# tissuelab_sdk 0.1.14 already depends on nibabel and pydicom.

numpy==2.4.6
Pillow==12.3.0
opencv-python==5.0.0.93
zarr==3.1.6
tifffile==2026.3.3
tiffslide==3.0.1
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
requests==2.34.2
PyYAML==6.0.3
tissuelab_sdk==0.1.14
scikit-image==0.26.0
sse-starlette==3.4.6

# macOS: PyPI CPU/MPS. Linux/Windows: CUDA 12.6 (not PyPI's default CUDA 13).
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.14.0; sys_platform == "darwin"
torch==2.14.0+cu126; sys_platform != "darwin"
torchvision==0.29.0; sys_platform == "darwin"
torchvision==0.29.0+cu126; sys_platform != "darwin"
tqdm>=4.66.0
huggingface_hub>=0.16.0

json_tricks==3.17.3
yacs==0.1.8
timm==0.4.12
einops==0.8.0
fvcore==0.1.5.post20221221
transformers==4.34.0
sentencepiece==0.1.99
ftfy==6.1.1
regex==2023.10.3
nltk==3.8.1
vision-datasets==0.2.2
cython==3.0.2
pycocotools>=2.0.8
diffdist==0.1
mup==1.0.0
accelerate==0.23.0
kornia==0.7.0
infinibatch==0.1.1
open-clip-torch==2.26.1

# detectron2 has no release newer than 0.6 (2021). Official main (Pillow 10+
# uses Image.BILINEAR) is the compatible source for Python 3.11 / Pillow 12.
# setup.py imports torch, so this cannot share the isolated `pip install -r`:
#   pip install --no-build-isolation git+https://github.com/facebookresearch/detectron2.git
#
# On Windows that build additionally needs DISTUTILS_USE_SDK=1, CL=/utf-8 and
# (optionally) TORCH_CUDA_ARCH_LIST, set after vcvars64.bat -- without them it
# fails inside pybind11/torch headers in ways that look unrelated. See the
# "Windows: three extra settings the detectron2 build needs" section in README.md.
